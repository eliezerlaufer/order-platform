apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "order-platform.componentLabels" (dict "component" "inventory-service" "context" $) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "order-platform.componentSelectorLabels" (dict "component" "inventory-service" "context" $) | nindent 4 }}
  ports:
    - name: http
      port: 8082
      targetPort: http
      protocol: TCP
