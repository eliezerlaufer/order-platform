apiVersion: v1
kind: ConfigMap
metadata:
  name: order-service-config
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "order-platform.componentLabels" (dict "component" "order-service" "context" $) | nindent 4 }}
data:
  SPRING_PROFILES_ACTIVE: k8s
