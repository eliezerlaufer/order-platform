apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "order-platform.componentLabels" (dict "component" "api-gateway" "context" $) | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "order-platform.componentSelectorLabels" (dict "component" "api-gateway" "context" $) | nindent 4 }}
  ports:
    - name: http
      port: 8888
      targetPort: http
      protocol: TCP
